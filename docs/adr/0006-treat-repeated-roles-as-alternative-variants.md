# Treat repeated roles as alternative Role Variants

Multiple lanes assigned the same Musical Role are primarily alternative harmonic performances, not layers expected to sound together. Orchestration budgets normal density around one active Role Variant per Role Family, gives each variant a stable Orchestral Identity and sibling Harmonic Path, and keeps variants compatible for occasional layering; dividing a role's density by its raw lane count would make each alternative too weak when performed alone.
